import React from "react";
import classNames from "classnames";
import styles from "./Footer.module.scss";

export default function Footer({}) {
  return <footer className={classNames(styles.footer)}></footer>;
}
